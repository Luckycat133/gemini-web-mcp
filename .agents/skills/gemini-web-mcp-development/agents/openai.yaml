interface:
  display_name: "Gemini Web MCP Development"
  short_description: "Build the agent-first multimodal Gemini Web gateway."
  default_prompt: "Use $gemini-web-mcp-development to inspect the current repository, define the contract and acceptance criteria, implement shared domain logic instead of duplicating primary and compact server behavior, preserve verifiable multimodal outputs, and run targeted plus repository validation before handoff."
