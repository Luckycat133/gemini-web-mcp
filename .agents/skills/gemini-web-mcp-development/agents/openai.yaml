interface:
  display_name: "Gemini Web MCP Development"
  short_description: "Review and evolve the agent-first Gemini Web gateway."
  default_prompt: "Use $gemini-web-mcp-development to inspect the final repository tree and recent repair commits, reproduce the requested defect, keep compatibility text and structured results aligned, treat accepted mutations as unverified until read-back evidence proves the target state, update both skill mirrors, and run focused plus maintained repository gates."
